<h1>My Notes</h1>

<h2><%= link_to "Create new note", new_note_path, :class => 'btb btn-default' %></h2>

<div>
  <% @note.each do |note| %>
    <h2><%= link_to note.title, note_path(note) %></h2>
    <p><%= note.text %></p>
  <% end %>
</div>
